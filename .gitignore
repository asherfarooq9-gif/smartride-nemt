# Python
__pycache__/
*.py[cod]
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Alembic compiled
backend/alembic/__pycache__/
backend/alembic/versions/__pycache__/

# Node
node_modules/
.next/
.turbo/
admin-dashboard/.next/
admin-dashboard/node_modules/

# Env — never commit real secrets
.env
*.env.local

# Docker volumes / data
mnt/

# IDE
.vscode/settings.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Flutter
apps/*/android/app/google-services.json
apps/*/build/
apps/*/.dart_tool/
apps/*/.flutter-plugins
apps/*/.flutter-plugins-dependencies

# Firebase service account credentials (contains private key — never commit)
backend/firebase-credentials.json
*firebase-adminsdk*.json

# Trained model artifacts — hundreds of MB, rebuilt from
# ai-services/triage/data/{train_distilbert,export_onnx}.py. Ship them via the
# triage_model volume / object storage, never through git.
*.onnx
ai-services/triage/data/distilbert-hospital-routing*/
ai-services/triage/model/

# Backups / temp
_archive/
_flutter_tmp/
_serveerr.txt
*.tsbuildinfo
