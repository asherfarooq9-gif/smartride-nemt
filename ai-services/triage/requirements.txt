fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pytest==8.2.0
pytest-asyncio==0.23.6

# Optional — only needed to serve the fine-tuned DistilBERT model. The service
# runs on keyword rules and falls back gracefully when these are absent or no
# model file is present (see model_infer.py).
numpy==1.26.4
onnxruntime==1.18.0
transformers==4.42.3
